name: Bot Scalping (5m/15m)

on:
  workflow_dispatch:
  schedule:
    - cron: "2-59/5 * * * *"

permissions:
  contents: read

jobs:
  run:
    runs-on: ubuntu-latest
    timeout-minutes: 4
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Run scalping bot
        env:
          BOTALIN_SERVER_PROFILE: training
          BOTALIN_STRATEGY: scalping
          BOTALIN_USER_LOGIN: server-scalping
        run: node share/strategy-lab-7q4m2v/server-autobot.mjs --once
