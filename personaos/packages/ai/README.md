# @personaos/ai

Reserved package boundary for the AI Layer.

This foundation stage intentionally does not implement AI agents, prompts, model routing, memory retrieval or provider calls. The package exists so future work can add OpenAI, Anthropic and local model adapters without changing the monorepo shape.
